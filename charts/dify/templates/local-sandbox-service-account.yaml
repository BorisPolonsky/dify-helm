{{- if .Values.localSandbox.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dify.localSandbox.serviceAccountName" . }}
  labels: {{- include "dify.labels" . | nindent 4 }}
    component: local-sandbox
  {{- if or .Values.localSandbox.serviceAccount.annotations (include "dify.ud.annotations" .) }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.localSandbox.serviceAccount.annotations (include "dify.ud.annotations" .) ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.localSandbox.serviceAccount.automountServiceAccountToken }}
{{- end }}
