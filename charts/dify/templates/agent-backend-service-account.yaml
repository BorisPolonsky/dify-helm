{{- if .Values.agentBackend.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dify.agentBackend.serviceAccountName" . }}
  labels: {{- include "dify.labels" . | nindent 4 }}
    component: agent-backend
  {{- if or .Values.agentBackend.serviceAccount.annotations (include "dify.ud.annotations" .) }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.agentBackend.serviceAccount.annotations (include "dify.ud.annotations" .) ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.agentBackend.serviceAccount.automountServiceAccountToken }}
{{- end }}
